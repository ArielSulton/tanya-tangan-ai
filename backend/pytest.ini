[pytest]
# Disable ROS2 launch_testing plugins — this workspace uses ROS2 Humble (pensyarat_ws)
# which injects pytest plugins that conflict with standard asyncio test runners.
asyncio_mode = auto
addopts = -p no:launch_testing -p no:launch_ros
